---
# - hosts: db
# - name: database assignment
#   ansible.builtin.import_playbook: ../static-assignments/db.yml

# - hosts: nginx
# - name: nginx assignment
#   ansible.builtin.import_playbook: ../static-assignments/uat-webservers.yml

# - hosts: artifactory
# - name: artifactory assignment
#   ansible.builtin.import_playbook: ../static-assignments/artifactory.yml hgnbj

 - hosts: sonarqube
 - name: sonarqube assignment
   ansible.builtin.import_playbook: ../static-assignments/sonarqube.yml

# - hosts: todo
# - name: Deploy the todo application
#   ansible.builtin.import_playbook: ../static-assignments/deployment.yml